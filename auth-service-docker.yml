
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://mysql:3306/cartigo_auth?createDatabaseIfNotExist=true
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

